import * as types from './mutation-types'

const state = {
  drawerListItems: {
    home: {
      text: 'Home',
      route: '/home',
      icon: 'home'
    }
  }
}

const getters = {}

const mutations = {
}

const actions = {
}

export default { state, getters, mutations, actions }
